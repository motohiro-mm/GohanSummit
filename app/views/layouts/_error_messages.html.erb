<% if model.errors.any? %>
  <div id="error_explanation" class="my-2 bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg">
    <ul>
      <% model.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
