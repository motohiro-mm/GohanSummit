<% set_meta_tags title: '献立の新規作成' %>
<% set_meta_tags description: '献立の新規作成ができます。' %>
<div class="mx-auto p-4 w-full">
  <%= render 'header_button', meal_plan: @meal_plan %>
  <div class="mt-4">
    <%= render 'form', meal_plan: @meal_plan %>
  </div>
</div>
